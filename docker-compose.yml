services:
  radare2-mcp:
    build: .
    volumes:
      - .r2_projects:/app/.r2_projects
      # Mount host binaries here
      # - /path/to/binaries:/data
    restart: unless-stopped

volumes:
  .r2_projects:
